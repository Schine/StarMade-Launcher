p Choose your player name or uplink with the Registry.
form
  input(type='text' ng-model='playerName' ng-disabled='$root.currentUser')
  button(ng-controller='AuthCtrl' ng-click='startAuth()') Uplink
